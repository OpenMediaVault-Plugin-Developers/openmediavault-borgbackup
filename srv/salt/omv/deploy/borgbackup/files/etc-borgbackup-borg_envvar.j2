{{ pillar['headers']['auto_generated'] }}
{{ pillar['headers']['warning'] }}
{%- for envvar in config.envvars.envvar | selectattr("reporef", "equalto", repo.uuid) %}
{{ envvar.envname }}="{{ envvar.envvalue }}"
{%- endfor %}
